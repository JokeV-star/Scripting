<!doctype HTML>
 <html lang="en>
let aantalLijnen =:string = prompt(message: "Uit hoeveel lijnen moet de driehoek bestaan?");
let uitkomst;

uitkomst =(aantalLijnen.toLowerCase() === "3") ? "#
                                                  ##
                                                  ###";
uitkomst =(aantalLijnen.toLowerCase() === "4") ? "#
                                                  ##
                                                  ###
                                                  ####";
uitkomst =(aantalLijnen.toLowerCase() === "5") ? "#
                                                  ##
                                                  ###
                                                  ####
                                                  #####";
